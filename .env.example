# Backend API URL (used by frontend for static export)
# Set to your deployed backend URL (e.g., Railway, Render)
# For local development, leave empty — Vite proxy or netlify.toml redirects handle it
NEXT_PUBLIC_API_URL=
